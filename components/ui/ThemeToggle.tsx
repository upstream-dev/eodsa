'use client';

/**
 * ThemeToggle removed — Avalon uses a single Chrome + Black theme.
 * Kept as a no-op export so any leftover imports do not crash.
 */
export const ThemeToggle: React.FC<{ className?: string }> = () => null;
